# ========================================
# SECURITY CONFIGURATION - REQUIRED
# ========================================
# Copy these to your .env file and provide secure values

# JWT Authentication (CRITICAL - MUST BE SET)
JWT_SECRET=your-super-secure-jwt-secret-key-min-32-chars-long
SESSION_SECRET=your-session-secret-key-for-secure-sessions

# IP Hashing Salt (CRITICAL for PII protection)
IP_SALT=your-unique-salt-for-ip-hashing-must-be-secret

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Database Security
# These should be stored in a secrets manager in production
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-supabase-service-key
SUPABASE_ANON_KEY=your-supabase-anon-key

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your-redis-password-if-required

# Server Configuration
NODE_ENV=production
PORT=3001
HOST=0.0.0.0

# ========================================
# SECURITY NOTES
# ========================================
# 1. NEVER commit real secrets to version control
# 2. Use environment-specific values (dev/staging/prod)
# 3. Rotate secrets regularly
# 4. Use a proper secrets management service in production
# 5. Monitor for unauthorized access attempts
# 6. Keep audit logs of security events